# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/namastesumalya/service/perform-technical-performance-audit).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Webpage Technical Performance Audit API
  description: Check a webpage URL for canonical, robots.txt, SSL status, and performance scores. Get
    issues and overall score for technical SEO review.
  version: 1.0.0
  contact:
    name: Sumalya APIs
    url: https://apyhub.com/namastesumalya/service/perform-technical-performance-audit
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: SEO
paths:
  /namastesumalya/api/audit:
    post:
      operationId: postNamastesumalyaApiAudit
      summary: Webpage Technical Performance Audit API
      description: Performs a technical performance audit for a webpage URL and returns the audited URL,
      tags:
      - SEO
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 100
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
