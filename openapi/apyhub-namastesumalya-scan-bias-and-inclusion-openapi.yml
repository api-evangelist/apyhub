# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/namastesumalya/service/scan-bias-and-inclusion).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Bias and Inclusion Scan API
  description: Review job description text for bias and inclusion issues. Returns a winner, rationale,
    and comparison matrix for hiring copy checks.
  version: 1.0.0
  contact:
    name: Sumalya APIs
    url: https://apyhub.com/namastesumalya/service/scan-bias-and-inclusion
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Artificial Intelligence
- name: HR
paths:
  /namastesumalya/api/bias-check:
    post:
      operationId: postNamastesumalyaApiBiasCheck
      summary: Bias and Inclusion Scan API
      description: Scans the submitted job description text and returns a structured assessment with a
        wi
      tags:
      - Artificial Intelligence
      - HR
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 1000
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
