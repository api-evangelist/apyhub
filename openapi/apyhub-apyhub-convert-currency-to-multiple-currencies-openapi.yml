# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/apyhub/service/convert-currency-to-multiple-currencies).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Convert Currency to Multiple Currencies API
  description: Convert a source currency into multiple target currencies with optional date-based rates.
    Returns pair keys with rates or false when unavailable.
  version: 1.0.0
  contact:
    name: ApyHub
    url: https://apyhub.com/apyhub/service/convert-currency-to-multiple-currencies
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Finance
- name: Standard Data
paths:
  /data/convert/currency/multiple:
    post:
      operationId: postDataConvertCurrencyMultiple
      summary: Convert Currency to Multiple Currencies API
      description: Converts one source currency into multiple target currencies for a given date and retu
      tags:
      - Finance
      - Standard Data
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 5
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
