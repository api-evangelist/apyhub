# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/chisleroff/service/check-rich-results).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Check Rich Results API
  description: Audit a webpage URL for rich result eligibility. Returns checked types, eligibility status,
    and missing or recommended fields for each result type.
  version: 1.0.0
  contact:
    name: Website Intelligence
    url: https://apyhub.com/chisleroff/service/check-rich-results
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: SEO
paths:
  /chisleroff/seo/rich-results:
    post:
      operationId: postChisleroffSeoRichResults
      summary: Check Rich Results API
      description: Checks a webpage URL for rich-results eligibility and returns a structured report of
        t
      tags:
      - SEO
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 100
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
