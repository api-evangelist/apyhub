# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/namastesumalya/service/technical-comparison).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Technical Comparison of Webpages API
  description: Compare multiple website URLs and get technical SEO data for each page, including metadata,
    headings, links, accessibility, and load time.
  version: 1.0.0
  contact:
    name: Sumalya APIs
    url: https://apyhub.com/namastesumalya/service/technical-comparison
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: SEO
paths:
  /namastesumalya/api/compares:
    post:
      operationId: postNamastesumalyaApiCompares
      summary: Technical Comparison of Webpages API
      description: Compares the technical and SEO characteristics of multiple websites from a list of
        URL
      tags:
      - SEO
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 100
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
