# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/apyhub/service/validate-aadhaar-number).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Validate Aadhaar API
  description: Validate a 12-digit Aadhaar number from a request body string and get a boolean result.
    Useful for KYC and identity form checks.
  version: 1.0.0
  contact:
    name: ApyHub
    url: https://apyhub.com/apyhub/service/validate-aadhaar-number
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Data Validation
- name: HR
paths:
  /validate/aadhaar:
    post:
      operationId: postValidateAadhaar
      summary: Validate Aadhaar API
      description: Validates an Aadhaar number sent in the request body. The response returns a JSON obje
      tags:
      - Data Validation
      - HR
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 5
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
