# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/chisleroff/service/extract-contact-information).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Extract Contact Information API
  description: Extract emails, phones, addresses, and social links from a webpage URL. Useful for lead
    enrichment, site audits, and contact validation.
  version: 1.0.0
  contact:
    name: Website Intelligence
    url: https://apyhub.com/chisleroff/service/extract-contact-information
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Data Extraction
- name: Data Validation
- name: Marketing
paths:
  /chisleroff/extract/contact:
    post:
      operationId: postChisleroffExtractContact
      summary: Extract Contact Information API
      description: Extracts contact details from a webpage identified by URL and returns the discovered
        c
      tags:
      - Data Extraction
      - Data Validation
      - Marketing
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 100
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
