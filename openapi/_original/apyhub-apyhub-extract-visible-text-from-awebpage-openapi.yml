# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/apyhub/service/extract-visible-text-from-awebpage).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Extract Text from Webpage API
  description: Extract visible text from a webpage as a string or line array. Useful for crawling, search
    indexing, and SEO checks.
  version: 1.0.0
  contact:
    name: ApyHub
    url: https://apyhub.com/apyhub/service/extract-visible-text-from-awebpage
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Data Extraction
- name: SEO
paths:
  /extract/text/webpage:
    get:
      operationId: getExtractTextWebpage
      summary: Extract Text from Webpage API
      description: Extracts the visible text from a webpage given its URL and returns it as a single stri
      tags:
      - Data Extraction
      - SEO
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 30
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
