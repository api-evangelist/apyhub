# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/apyhub/service/validate-swift-bic-code).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Validate SWIFT/BIC API
  description: Validate BIC/SWIFT codes and extract bank, branch, country, and location codes. Useful
    for payment forms and banking data checks.
  version: 1.0.0
  contact:
    name: ApyHub
    url: https://apyhub.com/apyhub/service/validate-swift-bic-code
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Data Validation
- name: Finance
paths:
  /validate/bic:
    post:
      operationId: postValidateBic
      summary: Validate SWIFT/BIC API
      description: Validates a BIC/SWIFT code submitted in the request body. The response returns a data
      tags:
      - Data Validation
      - Finance
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 5
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
