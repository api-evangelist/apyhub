# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/namastesumalya/service/generate-global-pricing-recommendations).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Global Pricing Recommendations API
  description: Generate country-specific subscription price recommendations from a base price, currency,
    and home country. Returns local prices and justification.
  version: 1.0.0
  contact:
    name: Sumalya APIs
    url: https://apyhub.com/namastesumalya/service/generate-global-pricing-recommendations
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: E-commerce Quick Tools
- name: Finance
paths:
  /namastesumalya/api/generate-pricing:
    post:
      operationId: postNamastesumalyaApiGeneratePricing
      summary: Global Pricing Recommendations API
      description: Generates pricing recommendations for multiple international markets based on a base
        s
      tags:
      - E-commerce Quick Tools
      - Finance
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 100
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
