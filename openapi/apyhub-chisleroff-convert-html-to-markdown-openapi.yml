# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/chisleroff/service/convert-html-to-markdown).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Convert HTML to Markdown API
  description: Convert a webpage URL into Markdown, with title, character count, and readability_success.
    Useful for content ingestion, archiving, and docs workflows.
  version: 1.0.0
  contact:
    name: Website Intelligence
    url: https://apyhub.com/chisleroff/service/convert-html-to-markdown
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Data Extraction
- name: Developer Tools
- name: File Conversion
paths:
  /chisleroff/tools/html-to-markdown:
    post:
      operationId: postChisleroffToolsHtmlToMarkdown
      summary: Convert HTML to Markdown API
      description: Converts the HTML content of a webpage into Markdown. You provide the webpage URL,
        and
      tags:
      - Data Extraction
      - Developer Tools
      - File Conversion
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 50
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
