# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/apyhub/service/validate-ifsc-bank-code).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Validate IFSC API
  description: Validate an IFSC code and get bank details like branch, city, state, and phone. Returns
    false when the code is not found or deleted.
  version: 1.0.0
  contact:
    name: ApyHub
    url: https://apyhub.com/apyhub/service/validate-ifsc-bank-code
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Data Validation
- name: Finance
paths:
  /validate/ifsc:
    post:
      operationId: postValidateIfsc
      summary: Validate IFSC API
      description: Validates an IFSC bank code by accepting an ifsc value in the request body and returni
      tags:
      - Data Validation
      - Finance
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 10
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
