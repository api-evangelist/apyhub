# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/apyhub/service/validate-indian-postcode).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Validate Indian Postcodes API
  description: 'Validate India Postcode Validator checks whether a postcode exists in its lookup table.
    Send a postcode string in the request body; spaces are ignored for matching. The response is intentionally
    simple: you get back a boolean data value. true means the postcode was found, and false mea'
  version: 1.0.0
  contact:
    name: ApyHub
    url: https://apyhub.com/apyhub/service/validate-indian-postcode
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Uncategorized
paths:
  /validate/postcodes/in:
    post:
      operationId: postValidatePostcodesIn
      summary: Validate Indian Postcodes API
      description: Validates an Indian PIN code by sending a postcode string in the request body and retu
      tags:
      - Uncategorized
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 10
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
