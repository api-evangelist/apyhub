# Derived by API Evangelist from ApyHub's own published service documentation
# (https://apyhub.com/llms.txt -> https://apyhub.com/namastesumalya/service/analyze-an-article).
# Paths verified callable against https://api.apyhub.com (401 = exists + gated).
# Request/response schemas are NOT published by ApyHub and are deliberately omitted.
openapi: 3.1.0
info:
  title: Extract and Analyze Content from Web Page API
  description: Analyze a web article from a URL and get entities, SEO score, content type, reading level,
    tone, and quality signals for review or automation.
  version: 1.0.0
  contact:
    name: Sumalya APIs
    url: https://apyhub.com/namastesumalya/service/analyze-an-article
servers:
- url: https://api.apyhub.com
  description: ApyHub API
security:
- apyToken: []
tags:
- name: Artificial Intelligence
- name: Data Extraction
- name: SEO
paths:
  /namastesumalya/api/analyze:
    get:
      operationId: getNamastesumalyaApiAnalyze
      summary: Extract and Analyze Content from Web Page API
      description: Analyzes an article from a web page URL and returns structured metadata about the page
      tags:
      - Artificial Intelligence
      - Data Extraction
      - SEO
      responses:
        '200':
          description: Successful response
        '401':
          description: Missing or invalid API key
      x-apyhub-atoms: 600
components:
  securitySchemes:
    apyToken:
      type: apiKey
      in: header
      name: apy-token
